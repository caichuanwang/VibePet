# Cutout Benchmark Fixtures

These 20 PNG fixtures are clean-room generated test images created for this repository. They do not contain third-party photographs or stock assets.

The manifest labels each image with one or more benchmark tags used by `CutoutBenchmark`: `clearSubject`, `edgeHard`, `lowContrast`, and `multiSubject`.
